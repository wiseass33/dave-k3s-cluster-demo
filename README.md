# dave-k3s-cluster-demo
This repository will house one or more projects containing services that will be deployed to the lab K3S cluster.  Actions will be used to Dockerize the application where the image is stored in GHCR.  The lab K3s deployment will reference the GHCR hosted image.  Helm will be used to install or upgrade the deployment.
